import React from "react";

export default function ProfileDebug() {
  return (
    <div
      style={{
        padding: "20px",
        backgroundColor: "#f0f0f0",
        textAlign: "center",
      }}
    >
      <h1>PROFILE DEBUG - ROUTE IS WORKING</h1>
      <p>
        If you see this, the /profile route is working and the issue is
        elsewhere.
      </p>
    </div>
  );
}
